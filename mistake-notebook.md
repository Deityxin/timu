# 错题本

这份文件用于长期积累算法题里的错因、模板、题型识别信号和背诵版总结。

建议用法：

- 做错一道题后，用“错题本模式”整理一条记录
- 复盘时重点看“根本错因”“下次识别信号”“背诵版”
- 隔一段时间回看，优先看重复犯错的类型

## 记录模板

### [日期] 题目名 / 文件名

- 题型：
- 原题或问题：
- 我这次错在哪里：
- 根本原因：
- 正确思路：
- 通用模板：
- 下次识别信号：
- 常见坑：
- 边界条件：
- 背诵版：

---

## 示例

### 2026-03-17 P1304 哥德巴赫猜想 / bits/test/103.cpp

- 题型：筛法 + 数学枚举
- 原题或问题：验证 4 到 n 的所有偶数都能表示成两个质数之和，并输出第一个加数最小的方案。
- 我这次错在哪里：一开始只会写筛法代码，但不理解为什么要建立 `vector<bool>` 质数表，也不理解为什么外层只枚举到 `i * i <= n`。
- 根本原因：对筛法的本质理解不够，只记住了写法，没有真正理解“先假设，再排除”和“只看根号范围”的原因。
- 正确思路：先用布尔表记录每个数当前是否仍可能是质数，再用埃氏筛把合数划掉。之后枚举每个偶数 `x`，从小到大枚举质数 `p`，检查 `x - p` 是否仍是质数。
- 通用模板：先筛出质数表，再用 `if (isPrime[x])` 做 O(1) 查询；涉及哥德巴赫或大量判质数时优先考虑筛法而不是每次现判。
- 下次识别信号：题目里出现“很多个数是否是质数”“反复判断质数”“1 到 n 范围内全部处理”时，优先想到筛法。
- 常见坑：把 `1` 当成质数；不理解 `i * i <= n`；不知道为什么从 `i * i` 开始筛；把 `vector<bool>` 当成二维矩阵理解。
- 边界条件：`0` 和 `1` 不是质数；最小偶数从 `4` 开始；布尔表大小要开到 `n + 1`。
- 背诵版：筛法就是先把所有数都当候选，再把合数划掉。合数一定有一个不超过根号的因子，所以只要筛到根号。需要反复判断质数时，先筛再查表。
