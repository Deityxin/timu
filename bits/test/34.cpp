#include <iostream>
#include <vector>
#include <string>
using namespace std ;
int main () {
    int n ; 
    cin >> n ; 
    vector<int> dig (n) ;
    vector<string> s_ ;
    for (int i = 0 ; i < n ; i++ ){
        cin >> dig[i] >> s_[i] ;
        size_t = s_.find('^') ;
        if(size_t)
        string s1 = s_.substr(size_t , 1) ;
    }
}